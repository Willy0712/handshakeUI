import Home from "../Components/LandingPage/Home";

type Props = {};

const HomePage = (props: Props) => {
  return (
    <div className="app">
      <Home />
    </div>
  );
};

export default HomePage;
